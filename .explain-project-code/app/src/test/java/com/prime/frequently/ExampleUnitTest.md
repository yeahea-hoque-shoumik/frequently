# ExampleUnitTest.kt

**Language:** Kotlin
**Package:** `com.prime.frequently`

---

## Class: com.prime.frequently.ExampleUnitTest

**Summary**
A scaffold local unit test generated by Android Studio. It runs on the development machine JVM (not a device) and verifies a trivial arithmetic assertion — serving only as a template and build-system smoke test.

**Instance Variables**

_No instance variables._

---

### `addition_isCorrect`

**Function Name:** `com.prime.frequently.ExampleUnitTest.addition_isCorrect`

**What** — Asserts that `2 + 2` equals `4`.

**Why** — Placeholder to confirm the unit test infrastructure (JUnit on the host JVM, Gradle test task) is wired correctly when the project is first created.

**How**
1. Calls `assertEquals(4, 2 + 2)` — fails if the result is not 4, passing otherwise

**Insights**
- This test provides zero coverage of application logic and should be replaced with real unit tests (e.g., for `FrequencyUtils.beatHzToCategory`, `TimeUtils.secondsToMmSs`, or ViewModel logic)
- Unlike `ExampleInstrumentedTest`, this test does not require a device or emulator — it runs during `./gradlew test`

---
